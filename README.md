
  # DriveFlow

  This is a code bundle for DriveFlow. The original project is available at https://www.figma.com/design/yT0IWtbTrhzyYE8gFnwcE3/DriveFlow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  